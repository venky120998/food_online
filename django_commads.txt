source env/bin/activate

django-admin startproject food_online_main .
python manage.py
python manage.py runserver
git add .	Stages changes in the current directory and its subdirectories.
git add -u	Stages modified and deleted files, but not new files.
git add -A	Stages all changes (new, modified, and deleted files) across the entire repository.

python manage.py migrate [To apply changes to the database.]
python manage.py createsuperuser
python manage.py collectstatic


        (user name) 
sudo -u postgres psql
postgres=# alter user postgres with password '12345';
quit

 git status
 git add -A
 git commit -m "homepage_setup, postgre config and .env sample"
 git push origin main
 
python manage.py startapp accounts

#### before making migrations after deleting db deactivate env and activate again
python manage.py makemigrations








###########
HTML
{% include 'includes/header.html' %}
{% include 'includes/footer.html' %}
{% extends 'base.html' %}
{% block content %}
{% endblock %}     
{% load static %}
"{%url 'home' %}" ### ahref lnk pages urls.py
"{% static 'js/scripts.js' %}" ### redirect to the static contents
{{form.password}}   ###to intergrate the fields in form
{% csrf token %}    ### When using post method
action = "{% url 'register_user' %}"  ### form actions
enctype="multipart/form-data" ### When ever adding files from to the form <form action>

### for field error
<ul class="errorlist">
        {% for field in form %}
                {% if field.errors %}
                        {% for error in field.errors %}
                                <li style="color: red;">{{error}}</li>
                        {% endfor %}
                {% endif %}
        {% endfor %}
        <li style="color: red;">{{form.non_field_errors}}</li>
</ul>  

# for showing sucess message
{% if messages %}
    {% for message in messages %}
        <div class="alert alert-{{message.tags}} show-notification" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
           <strong>{{message}}</strong>
        </div>
    {% endfor %}
{% endif %}